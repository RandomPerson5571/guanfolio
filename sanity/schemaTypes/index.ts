import { blogPost } from "./blogPost";
import { portfolioSettings } from "./portfolioSettings";
import { project } from "./project";
import { resume } from "./resume";

export const schemaTypes = [portfolioSettings, resume, project, blogPost];
